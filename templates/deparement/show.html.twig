{% extends 'base.html.twig' %}

{% block title %}Deparement{% endblock %}

{% block body %}
    <h1>Deparement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ deparement.id }}</td>
            </tr>
            <tr>
                <th>Name_dept</th>
                <td>{{ deparement.nameDept }}</td>
            </tr>
            <tr>
                <th>Email_dept</th>
                <td>{{ deparement.emailDept }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_deparement_index') }}">back to list</a>

    <a href="{{ path('app_deparement_edit', {'id': deparement.id}) }}">edit</a>

    {{ include('deparement/_delete_form.html.twig') }}
{% endblock %}
