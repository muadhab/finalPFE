{% extends 'base.html.twig' %}

{% block title %}Comparer{% endblock %}

{% block body %}
    <h1>Comparer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comparer.id }}</td>
            </tr>
            <tr>
                <th>Filename1</th>
                <td>{{ comparer.filename1 }}</td>
            </tr>
            <tr>
                <th>Filename2</th>
                <td>{{ comparer.filename2 }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_comparer_index') }}">back to list</a>

    <a href="{{ path('app_comparer_edit', {'id': comparer.id}) }}">edit</a>

    {{ include('comparer/_delete_form.html.twig') }}
{% endblock %}
