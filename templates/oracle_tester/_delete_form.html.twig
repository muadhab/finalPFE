<form method="post" action="{{ path('app_oracle_tester_delete', {'id': oracle_tester.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ oracle_tester.id) }}">
    <button class="btn">Delete</button>
</form>
